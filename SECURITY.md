# セキュリティポリシー

このプロジェクトはセキュリティツールです。**自分自身が漏洩源や攻撃経路になってはならない**という
前提で作っています。脆弱性を見つけたら、下記の手順で知らせてください。

## 報告のしかた

**公開 Issue には書かないでください。**

GitHub の **Private vulnerability reporting** を使います。

1. このリポジトリの **Security** タブを開く
2. **Report a vulnerability** を押す
3. 再現手順・影響・想定される攻撃者像を書いて送信

このやり取りは修正が公開されるまで非公開です。

## 対応の目安

| 段階 | 目安 |
|---|---|
| 受領の連絡 | 3 日以内 |
| 影響範囲の一次評価 | 7 日以内 |
| 修正の公開 | 重大度によるが、原則 90 日以内 |

個人が管理しているプロジェクトのため、商用サポートのような応答速度は保証できません。
報告者のクレジットは、希望があれば GitHub Security Advisory に記載します。

## サポート対象

| バージョン | 状態 |
|---|---|
| `main` ブランチ | 対象 |
| v2.x（リリース後） | 対象 |
| v1.x（bash / PowerShell 実装） | 対象外。深刻な問題があれば v2 で対応します |

## 対象になるもの・ならないもの

**対象になるもの（例）**

- 検出したシークレットの値が、レポート・トレース・LLM へのプロンプトに漏れる（設計書 §19.2 違反）
- `process` transport の隔離をすり抜けて、レビュー対象リポジトリが書き換えられる（同 §9.7 / P3 違反）
- レビュー対象のコード内の文字列が、コマンドやシェルとして解釈される
- API キーがログ・レポート・例外メッセージに平文で出る
- スキャナやツールの失敗が「指摘ゼロ」として成功扱いになる（同 §7.2 / P9 違反）

**対象にならないもの**

- `tests/fixtures/vulnerable-app/` や評価用データセットの脆弱性。
  **意図的に脆弱なコード**で、検出できることを確かめるための材料です。
- LLM が脆弱性を見逃した／誤検知したという報告。これは精度の問題なので、
  通常の Issue（できれば再現できる候補つき）でお願いします。
- プロンプトインジェクションによって LLM の判定が誤誘導されること自体。
  **完全な防御はできない**と明記しており（設計書 §19.4）、多層の緩和策を入れています。
  ただし、緩和策を無効化できる経路（デリミタのエスケープ漏れなど）は対象です。
- 対象コマンドや外部エンドポイント側の脆弱性。提供元に報告してください。

## このツール自体のセキュリティモデル

何が外部に送信され、何が送信されないかは [docs/security-model.md](docs/security-model.md) に
まとめています。`security-checker review --dry-run` で、送信前に送信予定の内容をすべて確認できます。

## 行動規範に反する言動について

セキュリティの問題ではなく、嫌がらせ等の行動規範違反については
[CODE_OF_CONDUCT.md](CODE_OF_CONDUCT.md) を参照してください。
